# Bassline Customs — Starter Website

Files added:

- `index.html` — main landing page
- `css/styles.css` — site styles
- `js/main.js` — small client behavior (form -> mailto)

How to preview:

1. Open `index.html` in your browser (double-click or use a local server).

Next suggestions:

- Provide a logo (PNG/SVG) to replace the text brand.
- Provide preferred colors and any copy changes.
- Supply gallery images to replace the Unsplash placeholders.
